import express from 'express';
import WebSocket from 'ws';
process.env.DERIV_APP_ID='348AuAfk8ZpsbSW8Whqc3';process.env.DERIV_CLIENT_ID='348AuAfk8ZpsbSW8Whqc3';process.env.DERIV_REDIRECT_URI='https://goonfx.com/';process.env.FRONTEND_ORIGIN='https://goonfx.com';
const {default:backend}=await import('../backend/server.js');const app=express();
const MARKET_WS='wss://api.derivws.com/trading/v1/options/ws/public';
function marketRequest(payload,timeout=12000){return new Promise((resolve,reject)=>{const ws=new WebSocket(MARKET_WS);let done=false;const timer=setTimeout(()=>finish(reject,new Error('Deriv market feed timed out')),timeout);const finish=(fn,v)=>{if(done)return;done=true;clearTimeout(timer);try{ws.close()}catch{}fn(v)};ws.on('open',()=>ws.send(JSON.stringify(payload)));ws.on('message',raw=>{try{const d=JSON.parse(raw);if(d.error)return finish(reject,new Error(d.error.message||'Deriv market error'));finish(resolve,d)}catch(e){finish(reject,e)}});ws.on('error',e=>finish(reject,e));ws.on('close',()=>{if(!done)finish(reject,new Error('Deriv market connection closed'))})})}
app.get('/api/markets',async(_req,res,next)=>{try{const d=await marketRequest({active_symbols:'brief'});const markets=(d.active_symbols||[]).map(x=>({symbol:x.underlying_symbol,underlying_symbol:x.underlying_symbol,display:x.underlying_symbol_name||x.underlying_symbol,display_name:x.underlying_symbol_name||x.underlying_symbol,market:x.market,submarket:x.submarket,subgroup:x.subgroup,exchange_is_open:x.exchange_is_open,is_trading_suspended:x.is_trading_suspended})).filter(x=>x.symbol);res.json({ok:true,markets})}catch(e){next(e)}});
app.get('/api/markets/:symbol/ticks',async(req,res,next)=>{try{const count=Math.min(Math.max(Number(req.query.count||160),10),1000);const d=await marketRequest({ticks_history:req.params.symbol,count,end:'latest',style:'ticks'});res.json({ok:true,symbol:req.params.symbol,history:d.history||null,times:d.times||null,prices:d.prices||null})}catch(e){next(e)}});
app.use(backend);export default app;