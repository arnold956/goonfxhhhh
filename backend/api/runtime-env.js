// Runtime environment shim.
// Secrets and OAuth configuration must be supplied by the hosting environment.
// This module intentionally does not contain Deriv access tokens or session secrets.
export {};
